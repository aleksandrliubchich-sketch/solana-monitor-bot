# Solana Monitor Bot

Telegram bot to monitor SOL token purchases.

## Setup

1. Copy config.py.example → config.py (locally)
2. Add your keys
3. Deploy to Railway or run locally:
   pip install -r requirements.txt
   python app.py
